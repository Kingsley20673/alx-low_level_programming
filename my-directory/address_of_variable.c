#include <stdio.h>

/**
*
* main- show the address of my variable
* Return: 0.
*/

int main(void)
{
	int n;
	int m;
	printf("show the address of n: %p" , &n);
 	printf("show the address of m: %p", &m);
return (0);
}
